<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.taylor.cse.pig.quiqstiq">

    <application
        android:name=".PackageCollection"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage">

        </activity>
        <activity
            android:name=".GameSelection"
            android:parentActivityName=".Homepage">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LineSelection" />
        </activity>
        <activity
            android:name=".LineSelection"
            android:parentActivityName=".Homepage">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homepage" />
        </activity>
        <activity
            android:name=".LetterSelection"
            android:parentActivityName=".Homepage">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homepage" />
        </activity>
        <activity
            android:name=".WordSelection"
            android:parentActivityName=".Homepage">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homepage" />
        </activity>
        <activity
            android:name=".LilyPads"
            android:parentActivityName=".Homepage">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homepage" />
        </activity>
    </application>

</manifest>